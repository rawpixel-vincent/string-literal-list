{
  "name": "string-literal-list",
  "version": "1.0.5",
  "description": "an array for strings",
  "main": "index.js",
  "types": "index.d.ts",
  "exports": {
    ".": {
      "import": "./index.js"
    }
  },
  "license": "MIT",
  "scripts": {
    "test": "node stringList.test.js && tsc --project ./jsconfig.json",
    "build": "tsc --declaration --emitDeclarationOnly --allowJs index.js",
    "precommit": "npm test && npm run build && git add ./index.*",
    "prepare": "husky"
  },
  "author": "Vincent Baronnet",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/rawpixel-vincent/string-list.git"
  },
  "devDependencies": {
    "husky": "^9.0.11",
    "typescript": "latest"
  }
}
